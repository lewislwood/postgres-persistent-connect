Backup and Restore  with the following commands:

// Backup via dump All
 docker exec -t local-postgres -pg_dumpall -c -U admin> dump_`date +%d-%m-%Y"_"%H_%M_%S`.psql
// restore all
 cat dump.psql | docker exec -i local-postgrespsql -d test_db -U admin

